using System;

string name = "Peter";
for (int i = 0; i <= 3; i++)
{
    int currentAge = 10 * i;
    if ( currentAge < 18)
    
        Console.WriteLine($"You are {currentAge} years old. That's too young.");
    
    else
    
        Console.WriteLine($"My Name is {name} and I am {currentAge} years old.");
    

    if (true)
    {
        
    }
}

